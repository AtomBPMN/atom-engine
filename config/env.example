# Atom Engine Environment Variables Example
# Пример переменных окружения для Atom Engine

# Configuration file path
# Путь к файлу конфигурации
ATOM_CONFIG_PATH=build/config/config.yaml

# Instance name for this deployment
# Имя инстанса для этого развертывания
ATOM_INSTANCE_NAME=atom-engine-dev

# Base path for all relative paths
# Базовый путь для всех относительных путей
ATOM_BASE_PATH=/home/user/Projects/atombpmn/atom-engine/build

# gRPC configuration
# Конфигурация gRPC
ATOM_GRPC_HOST=localhost
ATOM_GRPC_PORT=27500

# REST API configuration
# Конфигурация REST API
ATOM_REST_API_HOST=localhost
ATOM_REST_API_PORT=27555

# Database configuration
# Конфигурация базы данных
ATOM_DATABASE_PATH=data/base

# Storage configuration
# Конфигурация хранилища
ATOM_STORAGE_DIRECTORY=data
ATOM_STORAGE_TYPE=badger

# Logger configuration
# Конфигурация логирования
ATOM_LOGGER_LEVEL=debug
ATOM_LOGGER_FORMAT=text
ATOM_LOGGER_DIRECTORY=logs
ATOM_LOGGER_MAX_SIZE=50
ATOM_LOGGER_MAX_AGE=7
ATOM_LOGGER_MAX_BACKUPS=5
ATOM_LOGGER_ENABLE_CONSOLE=true
